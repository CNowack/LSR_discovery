name: mgefinder_env
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.8
  - mgefinder
  - bwa          # Required for alignment
  - samtools     # Required for BAM processingy
  - pandas